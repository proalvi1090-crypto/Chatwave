# Chatwave
ChatWave is a full-stack real-time messaging app featuring private/group chats, image uploads via Cloudinary, push notifications, and Redis-backed presence/queues. The client uses React + Tailwind and the server uses Node/Express with socket handling for realtime messages. Run locally with docker-compose up (see server and client README sections).
